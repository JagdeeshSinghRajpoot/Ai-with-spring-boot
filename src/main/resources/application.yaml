spring:
  application:
    name: demo
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: codellama:latest
          temperature: 0.7
    openai:
      api-key: ${OPENAI_API_KEY}
        options:
          model: gpt-5-mini
    chat:
      client:
        enabled: false      
server:
  port: 8081


   